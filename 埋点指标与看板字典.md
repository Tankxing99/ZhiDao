# 埋点指标与看板字典（MVP）

## 1. 事件与字段
- start_question: { mode }
- question_next: { qid, stepDur }
- adaptive_extra: { qid, extra, top1, topN }
- adaptive_finish: { qcount, extra, dur }

后端响应debug（/recommendPlants）
- ENABLE_BAYES_UNC, LAMBDA_UNC, ENABLE_MMR, MMR_LAMBDA, TOP_M_FOR_RERANK
- mmrCostMs, candidateTotal, topMUsed

## 2. 指标计算
- 问卷完成率 = count(adaptive_finish)/count(start_question)
- 追加题触发率 = count(adaptive_extra)/count(start_question)
- 每题时长P50/P90 = percentile(question_next.stepDur, 50/90)
- 总问卷时长P50/P90 = percentile(adaptive_finish.dur, 50/90)
- 推荐多样性开关使用率 = ratio(ENABLE_MMR==true)
- MMR耗时P50/P90 = percentile(debug.mmrCostMs, 50/90)
- 候选规模监控 = percentile(debug.candidateTotal, 50/90)
- 安全：不安全曝光率（后续接入）

## 3. 告警阈值（建议）
- 每题切换P95 > 80ms：提醒并降采样（前端）
- 总时长P95 > 120s：提醒并提高停止阈值/减少maxExtra
- /recommendPlants P95 > 800ms：提醒并关闭MMR或提高LAMBDA_UNC
- 不安全曝光率 > 0.1‰：红色告警，必要时回滚

## 4. 看板布局建议
- 漏斗：start_question → adaptive_finish
- 时长：stepDur/总dur分位曲线
- 追加题：触发率与top1/topN稳定度分布
- 推荐：MMR开关使用率、mmrCostMs分位、候选规模

## 5. 备注
- 事件需先在「数据分析-自定义分析」中创建
- 字段类型：string/number/boolean；复杂对象请先flatten或转字符串
- 灰度阶段先采集+观测，再逐步设告警与自动降级策略



## 6. 后端debug字段看板建议
- debug.elapsed：/recommendPlants 内部耗时（P50/P95）
- debug.fallback：回退路径占比
- debug.ab.bucket：control/treatment 占比与对应指标对比
- debug.mmrCostMs：MMR阶段耗时分位

建议阈值：
- elapsed P95 > 800ms 告警；
- fallback 占比 > 5% 告警；
- mmrCostMs P95 > 30ms 告警（按数据量动态调整）。
