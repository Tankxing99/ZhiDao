# 抖音小程序--植道 开发指南

一、需求澄清与优化建议
1. 用户旅程：
   进入小程序 → 问卷弹窗（可跳过）→ 提交问卷 → 推荐列表 → 点击图文 → 跳到抖音商城商品页 → 返回小程序可继续浏览。
2. 关键优化点
   • 问卷弹窗首次进入出现；若用户点击“稍后再说”，把状态写入 storage，24h 内不再弹。
   • 问卷控制在 6 题以内，支持本地缓存，断网可继续填写。
   • 推荐逻辑前端做轻量级规则匹配即可（无须后端），规则以 JSON 配置下发，方便运营随时改。
   • 图文卡片使用抖音小程序 `<image>` + `<text>` 组合；跳转推荐：优先使用 `tt.navigateToMiniProgram` 跳转到目标小程序（小店/电商插件小程序）并传入商品详情 path；如需引导关注可用 `tt.openAwemeUserProfile`（需开通能力）。
   • 交易/订单数据：如使用电商插件小程序或在本小程序内闭环交易，可按电商能力接入回调/报表做转化统计；若仅跳转第三方小程序，是否能获取订单归因需按官方电商方案评估与对接。
   • 绿植图文需 CDN 加速，建议用抖音云托管静态资源。
   • 合规：问卷不收集敏感个人信息；跳转商城需在隐私政策里声明第三方链接。

二、技术选型
前端：抖音小程序原生（ttml/ttss/js），无需框架，包体更小。
后端：抖音云托管（函数服务 Node.js/TS）。
数据源与组件：
   • 配置（question_config、recommend_rules）：存于抖音云数据库（MongoDB 或 MySQL），前端首次拉取后本地缓存 24h，并带版本号做 SWR 刷新。
   • 绿植商品池（plants）：运营用外部 CMS 维护后同步至数据库；图片存 TOS 对象存储并走 CDN。
   • 用户答案（user_answer）：按匿名 openId/unionId 记录，必要字段建索引。
调用方式：
   • 小程序通过 `tt.createCloud({ envID, serviceID }).callContainer(...)` 调用后端；或开启域名访问走 HTTPS。
CI/CD：
   • 云端在线开发与发布，或用 Git/Docker 流水线；支持灰度、回滚与监控告警。

三、目录结构
/miniprogram
 ├─ pages
 │   ├─ index/          首页（弹窗+推荐列表）
 │   ├─ question/       问卷页
 │   └─ detail/         绿植详情（可选）
 ├─ components
 │   ├─ dialog/         弹窗组件
 │   └─ plant-card/     绿植卡片
 ├─ utils
 │   └─ recommend.js    推荐算法
 └─ cloud-samples       可选：示例请求代码（实际后端在抖音云函数服务维护）

四、关键代码示例（仅供逻辑参考）
1. 弹窗组件（components/dialog/index.ttml）
```ttml
<view class="mask" tt:if="{{visible}}">
  <view class="dialog">
    <text class="title">为你推荐最合适的绿植</text>
    <text class="desc">仅需 30 秒，完成小测试</text>
    <button class="btn primary" bindtap="goQuestion">开始测试</button>
    <button class="btn ghost" bindtap="close">稍后再说</button>
  </view>
</view>
```

2. 首页逻辑（pages/index/index.js）
```javascript
Page({
  data:{ list:[], dialogShow:false },
  async onLoad() {
    const last = tt.getStorageSync('dialog_last') || 0;
    if (Date.now() - last > 24*60*60*1000) this.setData({ dialogShow:true });
    this.loadPlants();
  },
  closeDialog() {
    tt.setStorageSync('dialog_last', Date.now());
    this.setData({ dialogShow:false });
  },
  goQuestion() {
    tt.navigateTo({ url:'/pages/question/index' });
  },
  async loadPlants() {
    const cloud = tt.createCloud({ envID:'your-env-id', serviceID:'your-service-id' });
    const { data } = await cloud.callContainer({
      path:'/listPlants',
      init:{ method:'POST', header:{ 'content-type':'application/json' } }
    });
    const resp = JSON.parse(data || '{}');
    this.setData({ list: (resp && resp.data) || [] });
  },
  jumpShop(e) {
    const { appid, path } = e.currentTarget.dataset; // 目标小程序
    tt.navigateToMiniProgram({ appId: appid, path });
  }
});
```

3. 推荐算法（utils/recommend.js）
```javascript
// 规则：光照、空间、养护难度 3 维度
export default function recommend(list, answers) {
  return list.filter(p => {
    return (answers.light === p.light || p.light === 'all') &&
           (answers.space === p.space || p.space === 'all') &&
           (answers.level === p.level || p.level === 'all');
  }).sort((a,b)=>b.score-a.score).slice(0,5);
}
```

4. 云函数 listPlants（抖音云 函数服务 Node.js）
```javascript
const { dySDK } = require('@open-dy/node-server-sdk');
module.exports = async function (params, context) {
  const svc = dySDK.context(context);
  const { openId, anonymousOpenid } = await svc.getContext();
  // TODO: 查询数据库（MongoDB/MySQL）筛选 onShelf=true 的植物
  return { ok: true, data: [] };
};
```

五、测试清单
☐ 问卷断网续答与失败重试
☐ 弹窗 24h 不再打扰逻辑（时间戳验证）
☐ 推荐规则正确性（多维条件/边界值）
☐ 前端→抖音云函数：dev/prod 环境调用（callContainer/域名）
☐ 埋点：tt.reportAnalytics 事件上报与后台校验
☐ 跳转链路：tt.navigateToMiniProgram 正确到达商品详情路径
☐ 性能：首屏 < 2.5s（3G），图片懒加载、CDN 命中率
☐ 合规：隐私弹窗、用户协议、能力开通前置验证

六、上线后运营
• 在小程序后台配置「搜索直达词」：绿植推荐、懒人植物。
• 使用抖音「小程序锚点」功能，在短视频里挂小程序入口。
• 埋点事件：start_question、finish_question、click_plant；如接入电商插件/闭环交易，再上报 pay_success（以官方回调/报表为准）。

如需更复杂的规则引擎、A/B 测试或用户体系，可再迭代 2.0 版本。

七、能力与版本要求（要点）
- 跳转其他小程序：使用 tt.navigateToMiniProgram；需在 app.json 配置 navigateToMiniProgramAppIdList（按官方文档要求）。
- 引导关注：tt.openAwemeUserProfile，需在平台开通对应能力后使用。
- 埋点：tt.reportAnalytics，先在“数据分析-自定义分析”创建事件并校验。
- 云后端调用：tt.createCloud(...).callContainer(...) 或启用域名访问（HTTPS）。
- 以官方文档为准，确保基础库版本满足上述能力要求。

八、环境与配置管理（建议）
- envID、serviceID：抖音云函数服务的环境与服务标识。
- API_BASE（可选）：如果开启域名访问，配置 HTTPS 域名。
- CDN_BASE：TOS+CDN 的域名，用于图片/静态文件。
- ANALYTICS_ENABLED：埋点开关（灰度期间可关闭）。
- 统一在 config.js 中集中管理，并随构建环境切换（dev/prod）。

九、数据模型与索引建议（示例）
- question_config（或 recommend_config）
  - version: string
  - questions: array<{ id, text, options:[...] }>
  - updatedAt: number
  - 索引：version/updatedAt
- recommend_rules
  - version: string
  - rules: array<{ light, space, level, scoreWeight? }>
  - updatedAt: number
- plants
  - id, name, light, space, level, score, onShelf:boolean, imageUrl, tags:[]
  - updatedAt: number
  - 索引：onShelf、tags、updatedAt
- user_answer
  - openId/anonymousOpenid, answers:{...}, createdAt:number
  - 索引：openId/anonymousOpenid、createdAt

十、部署/灰度/回滚与监控
- 开发/联调：本地→云端调试；完成后发布到 dev。
- 上线：prod 环境灰度发布，观察关键指标后全量；支持回滚到指定版本。
- 监控：关注接口耗时、错误率、QPS；埋点事件漏斗；CDN 命中率与图片体积。

十一、埋点落地建议
- 事件建议：start_question、finish_question、click_plant、view_detail、jump_miniprogram；如接入电商闭环，再上报 pay_success。
- 使用示例：
```js
// utils/analytics.js
export function logEvent(name, params={}) { tt.reportAnalytics(name, params); }
```
- 在“数据分析-自定义分析”中创建并验证事件。

十二、后续演进
- 规则引擎增强（权重/区间/多维打分）、A/B 测试、用户画像与召回、服务端召回+前端重排。
  