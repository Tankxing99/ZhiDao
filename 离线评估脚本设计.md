# 离线评估脚本设计（骨架）

目标：提供可复用的离线评估框架，对比不同参数组合（λ_unc、λ_mmr、Top-M、τ、M）在历史数据或模拟数据上的NDCG/ILAD/覆盖度等指标表现。

## 1. 数据输入
- 历史问卷与行为数据：user_answer（answers, createdAt）
- 植物库：plants（onShelf, tags, safetyFlags, updatedAt）
- 真实反馈（可选）：点击/收藏/加购（若暂缺，用“规则真值”或运营标注代替）

## 2. 评估指标
- 排序：NDCG@5/10、MAP@K
- 多样性：Intra-list Diversity（基于标签/embedding）、Catalog Coverage
- 安全：不安全曝光率（应≈0）
- 性能：Top-M 重排时间估计

## 3. 评估流程（伪代码）
```
for param in grid(λ_unc, λ_mmr, TopM, τ, M):
  for user in sampled_users:
    answers = load_answers(user)
    candidates = load_plants()
    scored = base_score(answers, candidates)
    if ENABLE_BAYES_UNC: scored = apply_uncertainty(scored, λ_unc)
    reranked = MMR(scored.top(TopM), λ_mmr)
    metrics.accumulate( evaluate(reranked, ground_truth[user]) )
  report[param] = metrics.summary()
```

## 4. 输出
- 每组参数的指标摘要（表格/CSV）
- 指标分位曲线（可选）
- 最优参数集合与推荐参数

## 5. 工程化建议
- Node.js 脚手架：scripts/offline_eval/*（JS或TS皆可）
- 数据加载适配器：从抖音云导出或本地JSON模拟
- 结果写入 CSV/JSON；README 说明如何运行

## 6. 下一步
- 先用本地JSON数据跑通骨架（不依赖生产库）
- 加入抽样与随机种子，保证复现实验
- 如有真实反馈数据，再引入MAP@K/点击率重放评估

