## 20250819 核心算法优化方案（自适应问卷 + 学术化推荐）

作者：Augment Agent（基于《评估claude算法对比方案.md》与现有代码架构）

---

### 0. 背景与目标
- 现状：小程序已实现简化的动态问卷（3-5题）与权重加权的推荐逻辑；新方案提出分层深入问卷与更丰富的画像维度。
- 目标：在不牺牲现有“高完成率/低时长”优势前提下，引入“自适应问卷（VOI）+ 学术化推荐（贝叶斯权重、多目标与多样性）”，形成可落地、可验证的优化方案。
- 原则：安全为硬约束；准确性与多样性平衡；尽量复用现有数据结构与接口；通过A/B实验以量化指标驱动上线。

---

### 1. 自适应问卷算法设计（VOI/不确定性触发）

#### 1.1 理论基础
- Computerized Adaptive Testing（CAT/IRT）思想：按信息增益自适应出题。
- 信息增益/VOI（Value of Information）：提问的“期望收益”= 预期Top-N稳定性提升或排序质量（如NDCG@K）的期望增益。
- 不确定性估计：对权重w或候选得分S引入分布（如高斯先验/Monte Carlo），以“Top-N稳定概率/分数方差”度量不确定性。

#### 1.2 评分与不确定性建模（用于问卷停止和问题选择）
- 基础打分（与现架构一致）：S(p|a, w) = w_env·E(p|a) + w_aes·A(p|a) + w_care·C(p|a) + w_saf·Z(p|a)
- 贝叶斯先验：w ~ N(μ0, Σ0)，μ0可取[0.4,0.3,0.2,0.1]，Σ0=diag([0.1,0.1,0.1,0.05])
- 采样评估：从w分布采样M次（M=100），计算每次的Top-N；Top-1/Top-N稳定度=主导结果出现的频率。

#### 1.3 自适应问卷流程
- 初始阶段：固定问基础3题（light, space, level）。
- 计算不确定性：
  - 若Top-1稳定度≥0.90且Top-N稳定度≥0.95 → 停止问卷，进入推荐；
  - 否则进入“问题挑选”。
- 候选问题集合Q：安全（pets/children）、审美扩展、功能需求、生活习惯等。
- VOI估计（启发式）：
  - 对每个候选q∈Q，枚举其可能答案r∈R_q，更新w后重算Top-N稳定度或NDCG@K；
  - VOI(q) = E_r[ metric_after(q=r) - metric_now ]；取VOI最大者且VOI>τ（阈值）。
- 触发顺序与规则：
  1) 若安全冲突概率高（启发式：有宠物/有孩概率>0.5），优先问安全题；
  2) 若Top-N差距小且审美权重方差较大，优先问审美细化题；
  3) 若养护难度/频率不确定，问养护相关题；
  4) 每次仅追加1题，重复“稳定度/VOI”评估，直到满足停止条件或题目上限（再加≤3题）。

#### 1.4 自适应问卷伪代码（前端/本地执行为主）
```
initialize answers = ask_basic(light, space, level)
post_w ~ N(mu0, Sigma0)
repeat:
  stability = estimate_stability(post_w, answers, plants)  # MC over w
  if stability.top1 >= 0.90 and stability.topN >= 0.95: break
  Q = candidate_questions(answers)
  best_q, best_voi = None, 0
  for q in Q:
    voi_q = 0
    for r in possible_answers(q):
      answers' = answers ∪ {(q, r)}
      w' = update_posterior_w(post_w, q, r)  # 简化：调节w均值/方差
      stability' = estimate_stability(w', answers', plants)
      voi_q += P(r) * (metric(stability') - metric(stability))
    if voi_q > best_voi: best_q, best_voi = q, voi_q
  if best_voi <= tau or num_extra_questions >= 3: break
  a = ask(best_q)
  answers.add((best_q, a))
  post_w = update_posterior_w(post_w, best_q, a)
```
- 简化Posterior更新：根据题目→维度的映射，用小步长调整μ与Σ（如μ_dim += η·delta，Σ_dim *= decay），η∈[0.1,0.3]。
- metric可选：Top-N稳定度或离线估计NDCG@K的代理（见评估章节）。

---

### 2. 推荐算法优化（贝叶斯权重 + 多目标 + 多样性）

#### 2.1 目标与约束
- 硬约束：安全（宠物/儿童不安全）过滤。
- 主目标：相关性/适配性最大化（环境/养护/审美/功能）。
- 次目标：结果集多样性提升（避免同质化），覆盖主要风格/功能面。

#### 2.2 贝叶斯加权评分
- 加权打分：S(p) = E_w[w]^T f(p|a) ，并可加置信度惩罚：S'(p) = S(p) - λ_unc·Var_w[w^T f(p)]。
- 参数建议：λ_unc = 0.1~0.3，随在线波动调优。

#### 2.3 多目标合成与多样性重排
- 一阶段合成：S_base(p) = \sum_k w_k·score_k(p)；k∈{env, care, aes, func}。
- 多样性重排（MMR/xQuAD风格）：
  - MMR：R = argmax_{p∉R} [ λ·S_base(p) - (1-λ)·max_{q∈R} sim(p,q) ]
  - λ初值：0.7（相关性偏重），sim基于标签杰卡德/embedding余弦。
  - xQuAD可选：按方面覆盖度对“边际收益”加权，λ_xquad = 0.2~0.4。

#### 2.4 伪代码（服务端/路由 /recommendPlants）
```
plants = filter_onShelf(plants)
plants = filter_safety(plants, userProfile)
mu, Sigma = posterior_w(answers)  # 若前端已估计，可随请求传入摘要
for p in plants:
  rel = dot(mu, features(p, answers))
  var = quad_form(Sigma, features(p, answers))
  p.score = rel - lambda_unc * var
# 相关性初排
L = sort_by_score(plants)
# 多样性重排（MMR）
R = []
while len(R) < topN and L not empty:
  pick p maximizing lambda*score(p) - (1-lambda)*max_{q in R} sim(p,q)
  R.append(p); L.remove(p)
return R
```

---

### 3. 数据结构与API规范

#### 3.1 题库/选项结构（与现有兼容，新增元信息）
```
Question {
  id: string, type: 'single'|'multiple',
  text: string, options: Option[],
  tags?: string[]            # 题目维度标注
  affects?: { dim: 'env'|'aes'|'care'|'func'|'safety', weight: number }[]
}
Option {
  value: string, text: string,
  tags?: string[],
  shifts?: { dim: string, delta: number }[]  # 对权重均值的修正
}
```

#### 3.2 植物结构（补充多样性特征/功能标签）
```
Plant {
  id, name, onShelf,
  tags: string[],         # 环境/空间/风格/功能等标签
  safetyFlags: string[],  # pet_unsafe/child_unsafe
  embed?: number[]        # 可选，风格/图像/文本embedding
}
```

#### 3.3 API接口
- GET /questionConfig → { supportsDynamicQuestionnaire, questionBank, version }
- POST /recommendPlants
```
req: {
  answers: {id, value}[],
  topN?: number,
  mode?: 'fast'|'adaptive'|'precision',
  posteriorHint?: { mu:number[], sigma:number[] } # 可选
}
resp: {
  ok: true,
  data: Plant[],
  debug?: { usedPosterior:..., stability?:..., lambda_unc?:..., rerank:'MMR' }
}
```
- 可扩：POST /nextQuestion（若后端参与自适应）

---

### 4. 关键参数与调优策略
- 自适应问卷：
  - 采样次数M=100；稳定度阈值：Top1≥0.90 & TopN≥0.95；VOI阈值τ=0.01；最多追加题K=3。
  - Posterior步长η=0.2；方差衰减decay=0.9；安全题优先阈值P(safety)>0.5。
- 推荐阶段：
  - λ_unc=0.2（置信度惩罚）；MMR λ=0.7；sim=Jaccard(tags) 或 cos(embed)。
  - xQuAD（可选）：λ_xquad=0.3；方面分布按画像重要度归一。
- 调优：
  - 以离线NDCG@K、ILAD（多样性）和在线CTR/完成率为目标，采用网格/贝叶斯优化微调。

---

### 5. 评估指标与A/B测试方案

#### 5.1 离线评估
- 排序指标：NDCG@5/10、MAP@K；
- 多样性：Intra-list Diversity（基于标签/embedding）、Catalog Coverage；
- 安全：不安全曝光率（应≈0）。

#### 5.2 在线指标
- 问卷：完成率、P50/P90耗时、关键题弃答率、追加题触发率；
- 推荐：CTR@rec、二跳行为（收藏/加购/咨询）、次日回访率；
- 纠偏：结果页“换一换/改偏好”比率。

#### 5.3 实验设计
- 分流：现状对照组 vs 混合+自适应（50/50起步）；
- 决策阈值：NDCG@5（离线代理）≥+5%，在线完成率下降<3%，CTR@rec ≥+3% 即推进；
- 统计：使用连续监测+固定窗口双重确认；必要时采用序贯检验或贝叶斯A/B以降低样本开销；
- 样本量：按基线CTR与目标提升进行功效分析（α=0.05, power=0.8）。

---

### 6. 分阶段实施路线图
- 阶段MVP（1-2周）：
  - 前端加入自适应启发式：稳定度采样+VOI贪心选题（最多+3题）。
  - 服务端加入λ_unc置信度惩罚与MMR重排；埋点与debug输出。
- 阶段A/B（2-4周）：
  - 正式分桶，跑满样本；参数网格寻优（λ_unc, λ_mmr, τ, η等）。
  - 复盘离/在线指标，沉淀最佳参数与题库版本。
- 阶段学习化（4-8周）：
  - 引入轻量排序学习（如Pairwise/线性）迭代w；
  - 可选xQuAD与方面覆盖建模；
  - 后续探索：BPR/Embedding、图像/文本多模态增强。

---

### 7. 监控与告警
- 实时看板：
  - 问卷：完成率、平均题数、追加题触发/收益、P50/P90时长；
  - 推荐：CTR、Top-N多样性、NDCG代理、曝光量；
  - 安全：不安全曝光率（阈值>0.1‰告警，>0.5‰自动回滚到保守策略）。
- 告警策略：阈值+环比异常；回滚开关：禁用追加题/禁用MMR/提高λ_unc。

---

### 8. 工程对接与实现建议
- 复用现有结构：question_config与plants集合、pages/quiz/step前端逻辑、/recommendPlants后端路线。
- 最小改动路径：
  - 前端：新增稳定度估计模块（本地MC采样），VOI选题器与停止规则；
  - 后端：得分处引入mu/Sigma（若无则用默认），增加var惩罚与MMR重排，返回debug信息。
- 可解释性：在结果页展示匹配条目（光照/空间/审美/功能）与“多样性保障”提示。

---

### 9. 参考与理论依据（关键词）
- CAT/IRT（自适应测验）、Value of Information（决策与主动学习）
- Maximal Marginal Relevance（MMR, Carbonell & Goldstein, 1998）
- xQuAD多样性重排（Santos et al., 2010）
- 贝叶斯个性化/不确定性采样、Pairwise Learning to Rank、BPR（Rendle, 2009）

---

### 10. 风险与对策
- 完成率下滑：限制追加题数（≤3），阈值严控；优先安全/高VOI题；提供“直接看结果”。
- 算法不稳定：上线前离线回放+影子流；上线保守参数（高λ_unc/高λ_mmr）。
- 标签噪声：定期质检；高争议标签降权；embedding补充鲁棒性。
- 计算开销：前端采样M=100以内；后端MMR仅对Top-M候选（如M=50）重排。

---

结论：本方案以“混合+自适应深度”为主线，结合贝叶斯不确定性与多样性重排，保持现有体验优势的同时显著提升个性化与稳健性；通过明确的量化指标与A/B流程，确保上线可控、可复现、可迭代。
