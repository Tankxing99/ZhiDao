# 图片资源上云的指导文档（抖音小程序 + 抖音云 TOS + 火山引擎 CDN）

本文档提供一套标准化的图片上云流程与运维指引，确保资源上传、权限、安全、缓存与前端展示的端到端可靠性，避免出现“403/0KB/缓存未更新”等问题。

## 1. 目标与适用范围
- 目标：
  - 确保图片资源在抖音云 TOS 对象存储与火山引擎 CDN 上稳定可用
  - 规范权限与缓存策略，减少人为失误带来的故障
  - 提供自检与应急处理手册
- 适用范围：
  - 本项目的所有静态图片（尤其是 images/ 目录下资源）
  - 抖音小程序前端通过 CDN 域名访问图片的场景

## 2. 目录结构与命名规范
- 基础结构（建议）：
  - images/
    - festivals/        节日相关图片（pinyin 命名）
    - banners/          首页横幅/活动图
    - icons/            图标
    - placeholders/     占位图/兜底图
- 命名规范：
  - 一律小写、短横线或拼音命名，避免空格与中文
  - 固定扩展名：.jpg/.png/.svg 等
  - 慎用同名覆盖；若必须覆盖请执行 CDN 单文件刷新

## 3. 权限与安全策略（TOS）
- 公开读文件夹：
  - 在“对象存储 → 访问控制 → 公开读文件夹”中将 images/ 设为“公开读”，用于前端匿名 GET 访问
  - 注意：文件夹策略优先级高于单个对象 ACL。即使对象为“私有读”，位于公开读文件夹下仍可匿名访问
- 客户端 SDK 上传：
  - 默认“不支持”。仅在迁移或临时补传时短时开启，用完立即关闭，避免前端误写
- 服务端写入：
  - 常态化写入应由服务端/云端函数完成（避免客户端直接写入带来的安全与合规风险）

## 4. 上传流程（选择一种或组合）
### 4.1 控制台手工上传（小批量/紧急修复）
1) 打开抖音云控制台 → 对象存储 → 对应环境的桶 → 文件管理
2) 进入 images/ 及子目录（如 images/festivals/）
3) 点击“上传文件”，完成上传
4) 如为覆盖更新，上传后执行“CDN 单文件刷新”（见第 6 章）

### 4.2 后台/云函数写入（推荐，常态化）
- 由服务端接收业务端请求，上传到 TOS（或生成预签名 URL 供受控客户端使用）
- 优点：可控、可审计、易集中治理权限

### 4.3 迁移/批量上传工具（一次性迁移时）
- 若临时允许客户端 SDK 上传：
  - 在“公开读文件夹”设置中将 images 的“抖音客户端SDK上传”短时改为“支持”
  - 迁移完成后务必改回“不支持”
- 工具端必要校验与兜底（必做）：
  - 上传前：检查本地/临时文件路径存在且 size > 0
  - 稳定化临时文件：通过 FileSystemManager 读取后另存到 USER_DATA_PATH，再以上述路径上传（避免极少数格式导致 0KB）
  - 上传后：对云端对象执行一次 HEAD/轻量下载校验 Content-Length > 0；若为 0 立即重试一次（必要时换稳定临时文件后重传）
  - 详细日志：记录每个 key 的结果（OK/FAIL 与原因）

## 5. 前端使用规范（抖音小程序）
- 访问域名：一律使用 CDN 域名（如 https://cdn.iotvision.top），避免直接访问 TOS 源站
- URL 构造：通过统一工具方法（如 buildImageURL('festivals/duanwujie.jpg')）拼接
- 兜底处理：所有 <image> 标签添加 binderror，失败时替换为占位图（如 images/placeholders/plant-placeholder.png）
- 缓存策略：
  - 覆盖更新图片后，优先做 CDN 单文件刷新
  - 如暂不可刷新，可临时加 query 版本号（?v=yyyyMMdd-x），后续再移除

## 6. CDN 缓存刷新（火山引擎 CDN）
- 入口：火山引擎控制台 → CDN → 刷新预热 → 缓存刷新 → 任务类型=URL 刷新
- 步骤：
  1) 将需要刷新的 URL（每行一条）粘贴到输入框
  2) 点击“提交”，观察“提交成功”提示
  3) 可在“操作记录”查看任务进度与结果
- 典型场景：
  - 覆盖上传同名文件后，需刷新对应文件 URL
  - 出现旧缓存导致的小程序不更新时

## 7. 验收自检清单（每次上传/变更后执行）
- 源文件：本地文件尺寸与格式正确，无 0KB/损坏
- TOS：控制台文件大小符合预期，最后修改时间正确
- 浏览器直访：访问目标 CDN URL，HTTP 200 且 Content-Length > 0
- 小程序：开发者工具“清缓存并重新编译”，页面正常显示
- 若为覆盖更新：确认已执行 CDN 单文件刷新（或临时版本号已生效）

## 8. 常见问题与排查
- 403/AccessDenied：
  - 检查 images/ 是否配置为“公开读文件夹”
  - 若未用 CDN 而直接访问源站，可能需预签名或对象 ACL；建议统一走 CDN 域名
- 404/NotFound：
  - 对应 key 未上传或命名不一致（大小写/路径）。对照目录结构与代码映射核对
- 0KB 对象：
  - 迁移工具未做 size>0 校验或临时路径不稳定
  - 处理：覆盖上传正确文件 → 刷新 CDN → 如仍命中旧缓存，临时加版本号
- 刷新后仍不更新：
  - 确认提交的 URL 精确无误（含协议、域名、路径大小写）
  - 检查小程序端缓存，执行“清缓存并重新编译”

## 9. 变更与回滚
- 大规模替换图片：先灰度验证（小范围 URL 刷新），确认无误后批量刷新
- 临时版本号：仅作为应急手段；CDN 刷新完成后尽快移除
- 客户端 SDK 上传开关：仅迁移期短时开启，结束后立刻关闭

## 10. 运维与责任分工（建议）
- 前端：按规范引用 CDN 链接、添加错误兜底、在变更单中列出需刷新 URL 列表
- 后端/运维：执行 TOS 上传/覆盖与 CDN 单文件刷新；保留操作记录
- 代码与文档：对变更与经验在《开发进度记录.md》与本指南中留痕

## 11. 附录与参考
- 抖音小程序开放平台文档（总入口）：
  - https://developer.open-douyin.com/docs/resource/zh-CN/mini-app/introduction/usage-guide
- 抖音云开发（TOS）文档：
  - https://developer.open-douyin.com/docs/resource/zh-CN/developer/tools/cloud/introduction/summary
- 火山引擎 CDN 刷新/预热文档：
  - https://www.volcengine.com/docs/6454/71375

---

附：操作“黄金路径”SOP（最常用场景：覆盖更新图片）
1) 在 TOS 控制台覆盖上传同名文件（images/...）
2) 进入 火山引擎 CDN → 刷新预热 → URL 刷新，粘贴该文件的 CDN URL，提交
3) 本地浏览器直访 URL 验证 200 且非 0KB
4) 小程序开发者工具“清缓存并重新编译”验证页面正常
5) 如此前加过临时版本号参数，确认无误后从代码中移除该参数

